Integration of RAG Chatbot UI into Docusaurus Book. Target: Docusaurus Frontend + FastAPI Backend Connection. Focus: Fixing Chatbot Icon visibility and Frontend-Backend handshake. Success Criteria: Chatbot floating action button (FAB) is visible on all book pages. Chatbot opens a message window on click. Frontend successfully calls the FastAPI /chat endpoint. Support for "selected text" context passing to the Agent. Constraints: Must use React/Docusaurus architecture (hooks preferred). Backend URL must be configurable via environment variables. No blocking of Docusaurus core navigation. Not Building: New vector database logic (already exists). Authentication systems (beyond basic API keys).